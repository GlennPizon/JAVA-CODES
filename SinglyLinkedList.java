public class SinglyLinkedList{
      
   Node a = new Node(6);
   Node newNode = new Node(4);
   public Node head = a.newNode;

     // reference to head;
   
   //class Node that holds data and link to the next node;
   
   class Node{
      private int newNode;
      private Node next;
      // pointer to the next class;
      
      public Node(int newNode){
         this.newNode;
         a = head;
         newNode.next =a ;
         
         this.head=newNode;
      
      
      }
      
     
   }
   
}